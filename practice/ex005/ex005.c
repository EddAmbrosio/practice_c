#include <stdio.h>

int main() {
    /*int i = 55;
    char a, b;

    a = "1";
    b = "2";

    printf("%i", i);
    
   1º every variable must have a name
   2º every variable must have a type
   3º every variable must have a size
   4º every variable must have a value
   
   */

    return 0;
}